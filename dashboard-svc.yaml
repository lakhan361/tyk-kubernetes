apiVersion: v1
kind: Service
metadata:
  name: tyk-dashboard-lb
  labels:
    name: tyk-dashboard
spec:
  type: LoadBalancer
  ports:
  - name: tyk-dashboard
    port: 5000
    protocol: TCP
selector:
  app: tyk-dashboard
